// qeglfsemgdhmi_main.cpp — separate TU for the plugin entry point so the
// moc-generated metadata doesn't tangle with QEglFSEmgdHmiIntegration's
// own AUTOMOC output.

#include "qeglfsemgdhmi.h"

QT_BEGIN_NAMESPACE

// Provided in qeglfsemgdhmi.h via Q_PLUGIN_METADATA / Q_OBJECT. The class
// instance is constructed by Qt's plugin loader.

QT_END_NAMESPACE
