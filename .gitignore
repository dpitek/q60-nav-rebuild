# Claude Code tooling
.claude/

# Build outputs — large binaries and generated files not tracked
output/
# Geocoder SQLite DB — large binary built by scripts/build-geocoder-db.sh
output/geocoder/places.db
*.img
*.tar.gz
*.tar.xz
*.mbtiles
*.osm.pbf
*.db

# Docker layer caches
.build-cache/

# Logs
*.log
aqtinstall.log

# Kernel source tarball (downloadable separately)
linux-4.19.tar.xz

# Qt source tarball (downloadable separately)
deps/qt-everywhere-src-6.6.3.tar.xz

# MapLibre GL Native source (large — download separately; see deps/build-maplibre-i386.sh)
deps/maplibre-gl-native/

# Qt source and build trees (large — download separately; see deps/build-qt6-*.sh)
deps/qt6-src/
deps/qt6-build-host/
deps/qt6-build-i386/
deps/qt6-host/

# SQLite3 source and host build (built from source inside Docker)
deps/sqlite3-src/
deps/sqlite3-host/

# Sysroot (generated by build scripts)
deps/sysroot-i386/

# Valhalla source and build trees (large — see deps/build-valhalla-*.sh)
deps/valhalla/
deps/valhalla-build-i386/

# Linux kernel source tree (large — download separately; see kernel/README)
kernel/
kernel-2.6.37/
kernel-3.4/
kernel-3.4-src/

# Hardware reference materials (OEM PDFs, datasheets, large) — never commit
reference files/
"reference files"/

# Compiled rootfs binaries — built by scripts, not tracked
rootfs/opt/valhalla/bin/

# SpatiaLite/GEOS tarballs
deps/spatialite-*.tar.gz
deps/geos-*.tar.bz2

# macOS
.DS_Store
**/.DS_Store

# Editor
.vscode/
.idea/
*.swp
*.swo

# Compiled objects (should live in output/)
*.o
*.a
*.so
*.so.*

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
build.ninja
.ninja_deps
.ninja_log
install_manifest.txt

# JRE extracted by install-jre-rootfs.sh — large binary tree, rebuilt by script
rootfs/usr/
rootfs/etc/java-11-openjdk/

# Mesa EGL/swrast extracted by install-mesa-rootfs.sh — large binaries, rebuilt by script
rootfs/opt/nav/lib/
